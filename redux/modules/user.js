//Actions 

//Action Creator  

//initial State 
const initialState = {
    isLoggedIn: false
};
//reducer 
function reducer(state = initialState, action) {
switch (action.type) {
    default:
      return state;
  }
}
//reducer action 

//export

export default reducer;